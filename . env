Variable Name	Where to Get It	
`FIREBASE_PROJECT_ID`	`goldhunt-4e6f0`	
`FIREBASE_CLIENT_EMAIL`	From Firebase Console → Project Settings → Service Accounts → Generate new private key → copy `client_email`	
`FIREBASE_PRIVATE_KEY`	From the same JSON file → copy `private_key` (the entire long string with `\n`)	
